allprojects {
    buildscript {
        repositories {
            mavenLocal()
        }
    }
    repositories {
        mavenLocal()
    }
}

settingsEvaluated { settings ->
    settings.pluginManagement {
        repositories {
            mavenLocal()
        }
    }
}
